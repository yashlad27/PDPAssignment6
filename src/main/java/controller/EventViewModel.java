package controller;

/**
 * Simple view model for event data to be used with GUI components.
 */
public class EventViewModel {
    
    /**
     * Creates a new EventViewModel with default values.
     */
    public EventViewModel() {
        // Initialize with defaults
    }
} 